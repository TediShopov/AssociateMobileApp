<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Associate.Views.WordEnterAndWinningConditionOptionsPage">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Words Per Player"
                   FontSize="Medium"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand" />
            <Label BindingContext="{x:Reference Name=WordCountStepper}"  Text="{Binding Path=Value}">
                
            </Label>
            <Stepper x:Name="WordCountStepper"  Minimum="1"
                     Value="{Binding WordsToCreatePerPlayer}" Maximum="15" Increment="1" />

            <Picker Title="Select a Winnign condition"
        
        ItemsSource="{Binding WinningConditions}"
        ItemDisplayBinding="{Binding Name}" 
                    SelectedItem="{Binding SelectedWinnindCondition}"/>

            <Button Clicked="Button_Clicked"></Button>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>